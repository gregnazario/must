name: Deploy Docs

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - 'docs/**'
      - 'README.md'
      - 'CHANGELOG.md'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: '3.12'
      - uses: actions/setup-node@49933ea5288caeca864e2de1c7255fbc3498e3f9 # v4.4.0
        with:
          node-version: '22'
      - name: Build TypeScript
        run: cd site && npm ci && npm run build
      - name: Install dependencies
        run: pip install -r site/requirements.txt
      - name: Build site
        run: mkdocs build -f site/mkdocs.yml -d ../site_out
      - name: Add CNAME
        run: echo "must.ai" > site_out/CNAME
      - name: Upload artifact
        uses: actions/upload-pages-artifact@56afc609e7420260d3c3820fcb4c40bcddfd17e7 # v3
        with:
          path: site_out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
