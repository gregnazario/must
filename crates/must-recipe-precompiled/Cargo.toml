[package]
name = "must-recipe-precompiled"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
must-core = { path = "../must-core" }
must-cache = { path = "../must-cache" }
ureq = "3"
tar.workspace = true
flate2.workspace = true
zip.workspace = true

[dev-dependencies]
tempfile.workspace = true
