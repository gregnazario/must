[workspace]

[package]
name = "myapp"
version = "0.1.0"
edition = "2024"

[features]
cli = ["clap"]

[dependencies]
clap = { version = "4", features = ["derive"], optional = true }
env_logger = "0.11"
log = "0.4"
