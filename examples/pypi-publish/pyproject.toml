[build-system]
requires = ["hatchling"]
build-backend = "hatchling.backends"

[project]
name = "mylib"
version = "0.1.0"
description = "A sample Python library"
license = "MIT"
requires-python = ">=3.10"

[project.urls]
Repository = "https://github.com/myorg/my-python-lib"
